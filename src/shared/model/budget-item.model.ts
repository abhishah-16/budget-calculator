export class BudgetItem {
    amount: number;
    description: string
    constructor(amount: number, description: string) {
        this.amount = amount
        this.description = description
    }
}